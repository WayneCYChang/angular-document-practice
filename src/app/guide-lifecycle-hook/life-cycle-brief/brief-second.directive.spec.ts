import { BriefSecondDirective } from './brief-second.directive';

describe('BriefSecondDirective', () => {
  it('should create an instance', () => {
    const directive = new BriefSecondDirective();
    expect(directive).toBeTruthy();
  });
});
