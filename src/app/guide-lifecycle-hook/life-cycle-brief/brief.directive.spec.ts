import { BriefDirective } from './brief.directive';

describe('BriefDirective', () => {
  it('should create an instance', () => {
    const directive = new BriefDirective();
    expect(directive).toBeTruthy();
  });
});
